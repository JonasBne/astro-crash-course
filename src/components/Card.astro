---
interface Props {
    description: string;
    title: string;
}

const { description, title } = Astro.props;
---

<div class="flex flex-col p-4 m-4 rounded-md shadow-lg">
    <h3 class="text-lg font-bold mb-4">{title}</h3>
    <p class="font-light">{description}</p>
</div>
